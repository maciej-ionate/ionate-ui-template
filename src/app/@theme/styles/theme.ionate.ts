import { NbJSThemeOptions, DARK_THEME as baseTheme } from '@nebular/theme';

const baseThemeVariables = baseTheme.variables;

export const IONATE_THEME = {
  name: 'ionate',
  base: 'dark',
  variables: {

  },
} as NbJSThemeOptions;
